# Workspace Rules for ShopNest Frontend

- **Automatic GitHub Push**: Whenever any source files inside `onwear` are modified or created, the agent must immediately stage, commit, and push the changes to its GitHub repository (`onwear` -> `origin main`) without requesting permission from the user.
